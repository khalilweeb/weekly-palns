import clss from './Header.module.css';

const Header = () => {

    return (
        <header className={clss.header}>
            <h1>Weekly Planer</h1>
        </header>
    )

}

export default Header;